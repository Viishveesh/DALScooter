import React from 'react';

export default function Support() {
    return <div style={{ padding: '40px', textAlign: 'center' }}>
        <h1>Contact Support</h1>
        <p>This page will contain a form for message passing to a franchise operator.</p>
    </div>;
}